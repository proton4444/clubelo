# Database connection string
# Using Unix socket for local connection (faster and simpler for local dev)
# Format: postgresql://USER@/DATABASE?host=/path/to/socket
DATABASE_URL="postgresql://postgres@/clubelo?host=/var/run/postgresql"
# For remote connections, use: postgresql://USER:PASSWORD@HOST:PORT/DATABASE

# ClubElo API base URL (no trailing slash)
CLUBELO_API_BASE="http://api.clubelo.com"

# API server port
PORT=3000
